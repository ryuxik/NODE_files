#This should look something like what is in wrappingTest, this is still in progress because the code for the tracking is still not 
#finalized 

# import camFSMcontrol

# assert 'mainLoop' in dir(camFSMcontrol)
# assert callable(camFSMcontrol.mainLoop)

# camFSMcontrol.mainLoop()